using UnityEngine;
using System.Collections;

public class ResourceTrigger : Trigger {

    /*
    public override void TriggerAction(Bridge otherBridge)
    {
        if (otherBridge.crewManager == null) otherBridge.crewManager = otherBridge.GetComponent<SideViewerStats>().intMan.GetComponent<CrewManager>();
        otherBridge.crewManager.UseResources(false);
    }
    */
}

using UnityEngine;
using System.Collections;


/// <summary>
/// Defunct
/// </summary>
public class AnimationEvents : MonoBehaviour 
{
    /*
    public Hull targetHull;
    protected DUIController DUI;

    public void Start()
    {
        DUI = GameObject.Find("DUI").GetComponent<DUIController>(); 
    }

    public void SetTarget(Collider col)
    {
        targetHull = col.GetComponent<Hull>();
    }

    public void ShowGameOver()
    {
        DUI.AddEndGame();
    }

    public void DamageTargetHull(float damage)
    {
        if (targetHull)
            targetHull.Damage(damage, transform.position, gameObject);
        else
            Debug.Log("No Hull, dont call this!");
    }
    */
}

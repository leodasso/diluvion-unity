using UnityEngine;
using System.Collections;

/*
/// <summary>
/// Contains a position, name of an animation, and other stuff crew needs.  mostly aesthetic
/// </summary>
public class CrewSpot : MonoBehaviour {
	
	public bool visible = true;
	public string animString = "default";

	SpriteRenderer spriteRenderer;

	void Start() {

		//if there's a placeholder sprite, turn it off at start
		spriteRenderer = GetComponent<SpriteRenderer>();
		if (spriteRenderer) spriteRenderer.enabled = false;
	}
	
}
 * */
